{
  "name": "example",
  "version": "1.0.0-alpha.14",
  "private": true,
  "type": "module",
  "description": "Reference custom node-type package — a zero-capability greeting node and a capability-bearing scribe node, authored in TypeScript and built to the JS the loader dynamic-imports.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rad-orchestration/graph-engine": "1.0.0-alpha.14",
    "@types/node": "^22.19.17",
    "typescript": "^5.9.3"
  }
}
