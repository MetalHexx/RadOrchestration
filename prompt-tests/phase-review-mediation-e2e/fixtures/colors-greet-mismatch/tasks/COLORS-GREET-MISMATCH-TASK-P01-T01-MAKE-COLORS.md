---
project: COLORS-GREET-MISMATCH
phase: 1
task: 1
title: "Make Colors"
status: pending
skills: []
estimated_files: 1
---

# Task P01-T01 — Make Colors

## Intent
Implement `makeColors()` in `src/colors.js` that returns the array `['red', 'orange', 'yellow']` as an array of strings, in that exact order.

## File Targets
- `src/colors.js`

## Acceptance Criteria (FR-1)
- `makeColors()` returns a JavaScript array of exactly three strings: `'red'`, `'orange'`, `'yellow'`, in that order.
- Export is a named export (ESM).

## Tests
- Not required in this minimal handoff (harness validates via orchestrator mediation + re-review; no CI step).
