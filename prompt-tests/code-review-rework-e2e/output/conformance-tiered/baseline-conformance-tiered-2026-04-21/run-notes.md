# Inaugural baseline — pending

This run-notes file is a placeholder. The inaugural baseline is captured later by the outer session driving `_runner.md` end-to-end against the `conformance-tiered` fixture.

Once the outer session runs the harness:

- This file is overwritten with a structured summary of the run (see `_runner.md` → Outputs for the required sections).
- All 10 pass criteria must be explicitly marked green.
- `reports/` under the run folder will carry all four review docs (task first attempt, task re-review, phase review, final review).

Do NOT delete this file — its presence is what allows the `.gitignore` exception to re-include the baseline once the outer session completes the run.
