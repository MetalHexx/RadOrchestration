---
project: BROKEN-COLORS
phase: 1
task: 1
title: "Get Colors"
status: pending
skills: []
estimated_files: 1
---

# Task P01-T01 — Get Colors

## Intent
Implement a `getColors()` function in `src/colors.js` that returns the array `['red', 'orange', 'yellow']` in that exact order.

## File Targets
- `src/colors.js`

## Acceptance Criteria (FR-1)
- `getColors()` returns a JavaScript array of exactly three strings: `'red'`, `'orange'`, `'yellow'`, in that order.
- Export is a named export (ESM).

## Tests
- Not required in this minimal handoff (harness validates via orchestrator mediation + re-review; no CI step).
