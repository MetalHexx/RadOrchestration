# harness-files/tests/

## Purpose

Tests in this folder check the canonical source as a whole — agent frontmatter integrity, cross-skill reference resolution, naming conventions across the corpus — rather than any single skill or agent. Per-skill or per-agent tests live alongside their subject under each skill's own folder or co-located with each agent. If a test is not asserting about a whole-corpus invariant, it does not belong here.
