name: source-control
description: "Thin source control router. Invokes `radorch git commit` or `radorch git pr` using arguments from the spawn prompt. Never reads state.json."
model: haiku
user-invocable: false
tools: Read, Bash, TodoWrite
allowedTools:
  - Read
  - Bash
  - TodoWrite
skills:
  - rad-source-control
