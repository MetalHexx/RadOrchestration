{
  "hooks": {
    "SessionStart": [
      {
        "command": "node ${CLAUDE_PLUGIN_ROOT}/bin/radorch.mjs plugin-bootstrap --quiet --harness claude --plugin-root ${CLAUDE_PLUGIN_ROOT}"
      }
    ]
  }
}
