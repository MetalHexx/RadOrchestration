---
description: "Clean up a project's git worktree and branch"
agent: Orchestrator
---

# Cleanup Worktree

Clean up the git worktree for my current project. Remove the worktree directory
and delete the local branch if all work has been committed and pushed.
