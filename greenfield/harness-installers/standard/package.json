{
  "name": "@rad-orchestration/standard-source",
  "version": "1.0.0-alpha.9",
  "private": true,
  "type": "module",
  "description": "Source-side wrapper for the standard npm installer. Never published — npm pack is run against dist/ after build.",
  "scripts": {
    "build": "node build-scripts/build.js",
    "test": "node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "next": "^14.0.0",
    "tsx": "^4.0.0"
  },
  "dependencies": {
    "chalk": "^5.6.0",
    "@inquirer/prompts": "^8.3.0",
    "ora": "^9.3.0",
    "figlet": "^1.11.0"
  },
  "engines": { "node": ">=18" }
}
